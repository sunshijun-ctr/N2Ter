// 骨架 mock 数据入口（后续 api 层替换）

export { GENRES, SCHEMA_OPTIONS, mockAdaptationPlan, mockMessages } from './mock-constants'
export {
  mockNovels,
  mockScreenplays,
  mockEpisodesByScreenplay,
  getScreenplayForNovel,
  getOverviewScreenplay,
} from './mock-data'
